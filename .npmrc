# Expo SDK 57 ships a react / react-dom peer skew in its transitive web deps,
# and React Native Firebase declares broad peer ranges. Both trip npm's strict
# peer resolution. legacy-peer-deps keeps `npm install` / `npx expo install`
# working; the `react-dom` override in package.json pins the actual mismatch.
legacy-peer-deps=true
